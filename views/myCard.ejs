<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- <link rel="icon" href="https://cdn.iconscout.com/icon/premium/png-256-thumb/five-97-789300.png" type="image/gif" sizes="16x16"> -->
    <title>MyCard</title>
</head>
<body>
   


        <main>
            <% if(data.length > 0){ %>
                <%data.map(ele=>{%>
                    <div>
                        <img src="<%=ele.image_link%>" alt="" srcset="">
                        <h3><%=ele.name%></h3>
                        <p><%=ele.price%></p>
                      
                        <form action="\productDetials\<%=ele.id%>" method="get">
                         
                            <input type="submit" value="View Detials">
                        </form>
                    </div>
                    
                    <%});%>
            <% } else{ %>  
                <p>no Product uploaded</p>
            <% } %>
           
        </main>
   
</body>
</html>