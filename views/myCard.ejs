<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MyCard</title>
</head>
<body>
    <main>

        <% if(data.length > 0){ %>
            <%data.map(ele=>{%>
                <div>
                    <img src="<%=ele.image_link%>" alt="" srcset="">
                    <p><%=ele.name%></p>
                    <p><%=ele.price%></p>
                   
                    <form action="/productDetails/<%=ele.id%>" method="get">
                        
                        <input type="submit" value="View Detials">
                    </form>
                </div>
                
                <%});%>
        <% } else{ %>  
            <p>no photos uploaded</p>
        <% } %>

      
    </main>
</body>
</html>